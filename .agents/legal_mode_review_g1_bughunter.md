# G1 — Informe del BUG-HUNTER (Modo Legal M0–M3)

> Alcance: código NUEVO del Modo Legal + bordes con código viejo. Leído archivo por archivo
> (no el plan). Sin modificar producto. `tsc`/`tests` no re-ejecutados aquí
> (shell del entorno sin git/node operativos); los conteos citados son los del board/progress.
> `Bug real` = verificado por lectura con camino de ejecución concreto.
> `SOSPECHA` = plausible pero no probado; incluye el test que lo confirmaría.

## Veredicto: APROBADO CON CAMBIOS

Justificación: 2 hallazgos **Mayores** reales (B17, B18) dejan sin efecto garantías de
confianza del plan (D12 consentimiento; M4 guard de circulación en chat). No hay corrupción
de datos ni crash, y el núcleo (agente/wire/repos/corpus/stores) está sólido con defectos
menores acotados. Condición para G2: remediar B17 + B18 + B19 con tests de regresión en el
archivo dueño. Resto a backlog (B34 y R1 recomendados también antes de release).

## Hallazgos

| ID | Sev | Bug | Evidencia | Cómo reproducirlo | Fix sugerido | Archivo dueño |
|----|-----|-----|-----------|-------------------|--------------|---------------|
| B17 | Mayor (real) | El gate de consentimiento + preview de privacidad del Composer **nunca se monta**: `ChatPage` no pasa la prop `legal` y `buildLegalEphemeralSuffix` no consulta `LegalCase.consent`. El turno legal corre sin consentimiento. | `src/features/chat/ChatPage.tsx:264-276` (`<Composer … research={…}/>` sin `legal`); `src/features/chat/components/Composer.tsx:55` ("Lo cablea T27" — no ocurre), `:88-94`, `:238-292`; `src/features/chat/state/chatStore.ts:510-516` (`cases.get` sin leer `consent`) | Vincular caso sin `consent`, enviar mensaje en modo legal → el run se ejecuta igual; inspeccionar DOM: `legal-privacy-preview` ausente | Cablear en `ChatPageContent` la prop `legal={{redactionActive, redactedCounts, consentAccepted, onConsentChange}}` (leyendo el caso del `caseStore`) y/o chequear `consent` en `runTurn` antes del envío | `src/features/chat/ChatPage.tsx` (T27) + `src/features/chat/state/chatStore.ts` (T20) |
| B18 | Mayor (real) | `CitationGuardProvider` **no montado en ningún ancestro de producción** → `useCitationGuard()` devuelve el guard neutro (identidad) en `MessageList`/`MessageActions`. El guard de circulación en render/copiar (T21) es código muerto en el chat. | `src/features/legal/state/CitationGuardContext.tsx:105-108` (fallback neutro); `src/features/chat/components/MessageList.tsx:118,124-131`; `src/features/chat/components/MessageActions.tsx:38-39`; verificado: `App.tsx:104-111`, `AppShell.tsx`, `ChatPage.tsx`, `LegalPage.tsx` sin provider | Respuesta legal con cita inexistente → se renderiza y se copia SIN `[VERIFICAR]`; `citation-counters` nunca aparece en chat | Montar `CitationGuardProvider` con el índice del corpus en un ancestro del chat (p.ej. `ChatPageContent`, solo en modo legal) o aplicar `markLegalText` con índice directo en `MessageList` | `src/features/chat/ChatPage.tsx` (T27) / `CitationGuardContext.tsx` (T21) |
| B19 | Media (real) | `setLegalCase` acepta cualquier id sin validar; `caseStore.remove` **no desvincula conversaciones** → modo legal fantasma (scaffold + tools legales, sin brief) con caso inexistente/eliminado. | `src/features/chat/state/chatStore.ts:994-1005` (sin validación), `:598` (`legalMode = legalCaseId != null`), `:510-516` (get null → `undefined` silencioso); `src/features/legal/state/caseStore.ts:138-156` (remove sin unlink) | Crear caso → vincular chat → eliminar caso → enviar mensaje: corre en "legal" sin brief ni error | Al eliminar caso, desvincular conversaciones (`linkConversation(caseId, null)`); y/o en `runTurn` degradar `legalMode=false` si `cases.get` devuelve null | `src/features/legal/state/caseStore.ts` (T22) + `src/features/chat/state/chatStore.ts` (T20) |
| B1 | Media (real) | `closeToolCalls` emite `tool-end` para calls que **nunca tuvieron `tool-start`** (cortados por budget/abort antes de su turno en el loop). Par start/end asimétrico. | `src/domain/agent/runAgent.ts:360` (start dentro del loop) vs `:506-519` (close solo con end); consumidor `chatStore.ts:401-415` apendea resultado por cada end sin chequear start | Provider emite 3 tool-calls con `maxToolCalls: 1` → calls 2-3 reciben `tool-end` sin `tool-start` previo; contar eventos por call | Emitir también `tool-start` sintético en `closeToolCalls`, o documentar + test de tolerancia end-sin-start en el consumidor | `src/domain/agent/runAgent.ts` (T11) |
| B2 | Media (real) | IDs de tool-call **duplicados** en el mismo attempt → dos `tool-result` con el mismo `toolCallId` (historial persistido inválido; el wire lo oculta pero el disco queda corrupto). | `src/domain/agent/runAgent.ts:341` (`Set<string>`), `:345-397` (ambos duplicados ejecutan; 2º por caché), `:506-514` (close salta a ambos) | Fake provider que emite dos tool-call con mismo `id` en un paso → `runBlocks` con 2 results mismo id | Deduplicar `attempt.toolCalls` por id al recibirlos (conservar primero) o trackear ejecutados por índice | `src/domain/agent/runAgent.ts` (T11) |
| B6 | Media (real) | `sanitizeToolPairs` usa conteo **global** de results: con dos calls del **mismo id** y un solo result, el 2º call queda sin placeholder ni respuesta (wire inválido). Con ids únicos el algoritmo es correcto (verificado). | `src/domain/chat/buildWireMessages.ts:61,69` (`remainingResults` total, no "por venir") | wires `[A(call X), A(call X), T(X)]` → 2º X sin respuesta y sin placeholder | Consumir un "crédito" del conteo al procesar cada call, o deduplicar calls aguas arriba (mismo fix que B2) | `src/domain/chat/buildWireMessages.ts` (T11) |
| B26 | Media (real) | Doble toggle rápido de investigación pierde un toggle: `onToggleResearch(!researchMode)` captura el prop; dos clicks antes del re-render envían el mismo valor. | `src/features/chat/components/ModesMenu.tsx:118` | RTL: `researchMode=false`, doble click rápido → final `true` en vez de `false` | Toggle en el padre por lectura de store (callback sin arg) o deshabilitar durante el vuelo | `src/features/chat/components/ModesMenu.tsx` + `ChatPage.tsx` (T27) |
| B13 | Media (real) | `LegalCorpus.ensureIndex` memoiza solo por `(id, version)`, no por hash: contenido cambiado con misma versión (reinstall directo fuera de `sync`) devuelve índice rancio. | `src/adapters/legal/LegalCorpus.ts:141` (key), `:157-165` (memo), `:70-74` (invalidate solo desde sync) | `install` v1 → `ensureIndex` → `install` v1 modificado (mismo id/version) → `ensureIndex` devuelve índice viejo | Clave `id@version@hash`, o exponer `invalidate()` y llamarlo en los writers | `src/adapters/legal/LegalCorpus.ts` (T15) |
| B4 | Media (real) | `ephemeralTokens` reservado en la selección pero **excluido de `estimatedPromptTokens`** → el check de budget subestima el prompt real; además la recalibración (`real/estimated`) se infla artificialmente en turnos con brief grande. | `src/domain/agent/runAgent.ts:165,201-202`; `src/domain/agent/budget.ts:94-105` | Turno legal con brief ~2000 tokens y `maxTotalTokens` ajustado → `calibration` > 2 tras el 1º paso; wire puede exceder ventana pese a la reserva | Sumar `ephemeralTokens` al estimado para el budget check (manteniéndolo fuera de la selección) + test de deriva de calibración | `src/domain/agent/runAgent.ts` (T11) |
| B9 | Media (real) | Paridad real/fake solo **conductual**, no estructural: `IndexedDbLegalCases.get/list` no clonan (confían en el clon estructurado de IDB); Memory clona siempre. Un futuro cache en memoria delante de IDB rompería la invariante. | `src/adapters/storage/IndexedDbLegalCases.ts:44-53`; `src/test/fakes/MemoryRepos.ts:417-419` (`cloneLegal`) | Hoy sin repro (el contrato defensivo pasa en ambos por vías distintas) | Clonar en `get/list` de IDB igual que Memory (JSON deep clone, barato a este tamaño) | `src/adapters/storage/IndexedDbLegalCases.ts`, `IndexedDbLegalPacks.ts` (T13) |
| B34 | Baja (real) | `linkConversation(caseId, null)` barre **todas** las conversaciones con N updates secuenciales; un fallo a mitad deja desvinculación parcial sin continuación. | `src/features/legal/state/caseStore.ts:166-171` | Inyectar fallo en el 2º `update` con 3 conversaciones vinculadas → 1ª desvinculada, resto no, `error` seteado | `Promise.allSettled` + reporte, o evitar el barrido (ver B19) | `src/features/legal/state/caseStore.ts` (T22) |
| R1 | Baja (real, latente) | Doble marcado del guard: `MessageList` pasa a `MessageActions` el texto **ya marcado** y `MessageActions` vuelve a marcar para copiar. Benigno solo si `mark` es idempotente (declarado pero sin test que lo ate). | `src/features/chat/components/MessageList.tsx:125,174`; `src/features/chat/components/MessageActions.tsx:39`; `src/features/legal/state/CitationGuardContext.tsx:15-16` | Cuando B18 se arregle: `mark(mark(x))` con cita → ¿doble `[VERIFICAR]`? | Test de idempotencia `mark(mark(x))===mark(x)` + pasar texto crudo a copiar (marcar una sola vez) | `src/features/chat/components/MessageActions.tsx` (T21) |
| B31 | Baja (real) | `#/legal/:caseId` inexistente se selecciona a ciegas: sin error ni redirección, detalle vacío con URL que sugiere un caso. | `src/features/legal/LegalPage.tsx:58-60`, `:62`, `:149-151` | Navegar a `#/legal/no-existe` → lista + "seleccioná", URL con id fantasma | Tras `list()`, si `routeCaseId` no está en casos mostrar aviso o limpiar URL a `#/legal` | `src/features/legal/LegalPage.tsx` (T23) |
| B23 | Baja (real) | El consentimiento del `LegalStep` **no se persiste**: `finish()` guarda `enabled/setupCompleted` pero nada del consent (sin campo en `LegalSettings`). Es fricción sin efecto reutilizable. | `src/features/onboarding/components/OnboardingWizard.tsx:142-148`; `src/features/onboarding/state/wizardReducer.ts:136-142`; `LegalSettings` sin consent | Completar onboarding legal on + consent → settings sin rastro del consent; crear caso exige consent de nuevo | Documentar como gate de lectura, o persistir `consentAt` en settings | `OnboardingWizard.tsx` / `LegalStep.tsx` (T25) |
| B27 | Baja (real) | Menú: cierre afuera escucha `mousedown` (no `click`), y `Escape` cierra **sin devolver el foco** al botón. | `src/features/chat/components/ModesMenu.tsx:59-64` (sin `buttonRef.focus()`) | Test con `fireEvent.click(document)` no cierra; tras Escape el foco queda en body (a11y) | Devolver foco al botón al cerrar por Escape; considerar `pointerdown`+`click` | `src/features/chat/components/ModesMenu.tsx` (T27) |
| B28 | Baja (real) | `CaseLinkDialog.handleCreate` sin `.catch`: si `create()` lanzara (hoy devuelve null, pero es API pública), `saving` queda en true y hay rejection sin manejar. | `src/features/chat/components/CaseLinkDialog.tsx:119-131` | Hoy sin repro directo (store nunca lanza); cambio futuro lo activa | Agregar `.catch` que resetee `saving` + `formError` | `src/features/chat/components/CaseLinkDialog.tsx` (T27) |
| B29 | Baja (real) | El diálogo no resetea `saving`/`loadError` al (re)abrir: cerrar a mitad de creación deja spinner pegado al reabrir. | `src/features/chat/components/CaseLinkDialog.tsx:61-95` (apertura resetea title/formError, no saving/loadError) | Click crear → cerrar antes de resolver → reabrir → botón en loading | Resetear `saving`/`loadError` en el efecto de apertura | `src/features/chat/components/CaseLinkDialog.tsx` (T27) |
| B10 | Baja (real) | `listInstalled` (IDB) confía en el orden implícito del índice `installedAt` para el desempate por id en vez de ordenar en JS como el fake. | `src/adapters/storage/IndexedDbLegalPacks.ts:24-28` vs `MemoryRepos.ts:299-309` | Solo diverge si el motor no ordena ties por PK (fake-indexeddb sí lo hace) | Ordenar en JS tras `getAllFromIndex` | `src/adapters/storage/IndexedDbLegalPacks.ts` (T13) |
| B15 | Baja (real) | Carrera `syncFromManifest` vs `ensureIndex` en vuelo: el `invalidate()` a mitad de build es pisado por el build viejo al terminar (ventana de `getIndex()` rancio; el siguiente `ensureIndex` se recupera solo). | `src/adapters/legal/LegalCorpus.ts:70-74,157-165` | `ensureIndex` lento + `sync` concurrente con pack nuevo → `getIndex()` devuelve índice viejo una ventana | Token de generación en `buildIndex` (solo escribir caché si la época sigue vigente) | `src/adapters/legal/LegalCorpus.ts` (T15) |
| B37 | Baja (real) | `sw.js` solo bypasea pathnames con prefijo literal `/legal/`; bajo subpath (p.ej. `/app/legal/…`) los packs caerían en `cacheFirst` y quedarían pegados. | `public/sw.js:39` | Servir la app bajo subpath → packs cacheados viejos pese a `?v=hash` distinto en otra ruta | `includes('/legal/')` o derivar de la base real | `public/sw.js` (T15) |
| B20 | Baja (real) | `setResearchMode` y `setLegalCase` comparten `toggleSeq`: toggles concurrentes de dimensiones ortogonales (D17) se pisan el `publish` mutuamente (solo notificación a la lista, no el estado). | `src/features/chat/state/chatStore.ts:239,976-1005` | `setResearchMode` + `setLegalCase` concurrentes → uno omite `publishConversation` | Secuencias separadas o publish incondicional | `src/features/chat/state/chatStore.ts` (T20) |
| B33 | Baja (SOSPECHA) | Defaults globales no editables ni fijados por onboarding: `finish()` nunca guarda `defaultJurisdiction/defaultCourt/defaultMatter` y `WorkModeSection` los muestra read-only. La promesa "3 vías convergen / defaults globales" es parcial. | `OnboardingWizard.tsx:145-148`; `src/features/settings/components/WorkModeSection.tsx:39-48` | Workspace on → nuevo caso usa siempre defaults de fábrica, sin UI para cambiarlos | Confirmar diseño (Evaluator): o controles en `WorkModeSection`, o documentar fábrica fija | `WorkModeSection.tsx` / `OnboardingWizard.tsx` (T25) |
| B3 | Baja (SOSPECHA) | `tool-end` exactamente 1× por call ante `permissions.request` que rechaza: el `catch` (runAgent.ts:398-402) lo cubre, pero sin test que lo ate. | `src/domain/agent/runAgent.ts:398-402` | Test: `permissions.request` rechaza en el 2º de 3 calls → contar `tool-end` por id (esperado: 1 c/u, sin duplicados ni faltantes) | — (test de confirmación) | `src/domain/agent/runAgent.test.ts` (T11) |
| B5 | Baja (SOSPECHA) | Paso final `answerForced` cuyo modelo solo emite tool-calls → run `complete` con assistant vacío (sin fallback de texto). Preexistente también en research. | `src/domain/agent/runAgent.ts:448-451` | Forzar `answerForced` + provider que insiste en calls → mensaje final sin texto | Mensaje de síntesis mínima o reintento sin tools ya cubierto; confirmar con test | `src/domain/agent/runAgent.test.ts` (T11) |
| B7 | Baja (SOSPECHA) | Adyacencia call→result ante evicción parcial del historial: el sanitizador no reordena; si algún camino construyera `A1(X),A2,T(X)`, proveedores estrictos lo rechazarían. Hoy no encontré constructor (turnos atómicos + `splitTurns` por turnos). | `src/domain/chat/buildWireMessages.ts:58-91`; `selectHistoryByBudget.ts:186-198` | Test: historial con assistant-call cuyo result fue evictado + suffix → verificar placeholder adyacente al call | — (test de confirmación) | `src/domain/chat/buildWireMessages.test.ts` (T11) |
| B11 | Baja (SOSPECHA) | Atomicidad de cascada bajo fallos inyectados (IDB transaccional vs Memory secuencial). Sin cobertura en el contrato. | `IndexedDbLegalCases.ts:91-121` vs `MemoryRepos.ts:199-214` | Test: harness que falla en el 2º delete → IDB rollback total vs Memory parcial | — (test de confirmación) | `src/adapters/storage/legalContract.ts` (T14) |
| B16 | Baja (SOSPECHA) | `syncFromManifest` ante `listInstalled` que falla reinstala TODO (seguro pero costoso; posible reintento pesado en cada arranque con IDB corrupto). | `src/adapters/legal/LegalCorpus.ts:85-89` | Test: store que lanza en `listInstalled` → contar `http.request` (esperado: N packs) | Backoff o flag de reintento | `LegalCorpus.ts` (T15) |
| B21 | Baja (SOSPECHA) | Toggles del `ModesMenu` habilitados **durante un run**: el cambio no afecta al turno en curso (snapshot) pero la UI sugiere lo contrario. | `ChatPage.tsx:153-171` (sin `disabled={busy}`); `chatStore.ts:567-574` (snapshot) | Apagar legal a mitad de run → la respuesta sale legal igual | `disabled` por `busy` o nota visual | `ChatPage.tsx` (T27) |
| B22 | Baja (SOSPECHA) | `send()` sobre conversación archivada/eliminada: sin chequeo de `status`; `appendMessage` huérfano si la conversación desaparece entre `get` y `append` (depende de si IDB valida FK — no verificado). | `chatStore.ts:434-480` | Test: repo que devuelve null en `get` tras crear → `configurationError` sin mensaje huérfano (verificar); eliminar conversación mid-turn | — (tests de confirmación) | `src/features/chat/state/chatStore.ts` (T20) |
| B30 | Baja (SOSPECHA) | Turno legal con servicios incompletos (sin `legalCases`/`legalCorpus`): corre con scaffold legal pero sin brief ni tools — degradación silenciosa y confusa. | `src/app/services.tsx:102-114`; `chatStore.ts:510-545` | Services sin `legal*` + `legalCaseId` seteado → system con scaffold, wire sin brief | Aviso en UI o `legalMode=false` efectivo con log interno (sin secretos) | `src/features/chat/state/chatStore.ts` (T20) |
| B35 | Baja (SOSPECHA) | Timer `wallClock` de `analysisStore` vs `stop()`+nuevo run: el timer viejo podría abortar controllers del run nuevo en una ventana mínima (los caminos normales lo limpian; falta test de temporización). | `src/features/legal/state/analysisStore.ts:208-216,337-348` | Test con timers falsos: run1 con `maxWallClockMs` corto → `stop()` → run2 inmediato → el timer de run1 no debe tocar run2 | Guardar época en el timer o limpiar en `claimRun` | `src/features/legal/state/analysisStore.ts` (T22) |

## Verificado sin bug (para que G2 no lo reabra)

- `buildWireMessages` con ids únicos: tool-antes-de-call, call-sin-result y result-duplicado se manejan bien (placeholder adyacente / descarte 2º) — `buildWireMessages.ts:58-91`.
- `runAgent` paths `aborted/budget/failed/retry/context_length` cierran una sola vez (sin doble `tool-end`); `failedTwice` no pushea `stepMessage` (correcto, el final lo porta).
- Contrato real/fake: orden `(createdAt,id)` / `(at,id)` / `(installedAt,id)`, upsert packs, `update` preserva `createdAt` + bumpea `updatedAt`, cascada feliz, copias defensivas — `legalContract.ts` + `conversationContract.ts` (link/unlink).
- `LegalCorpus`: llamadas concurrentes comparten `pending`; `sync` nunca lanza; `expectedHash` liga manifiesto↔contenido; `bytes` fallback correcto.
- `chatStore`: draft (`conversationId null`) persiste `researchMode` + `legalCaseId` al crear (`:434-452`); `load()` restaura ambos (`:800-820`); `continue/regenerate/edit/delete` con tokens correctos.
- Onboarding: `WIZARD_STEPS` (4) + `stepLabel` + `StepIndicator` coherentes; `canAdvance` bloquea legal-on sin consent; `setLegal` limpia consent al apagar; `skipForNow` no corrompe (sesión resuelta, settings intactos).
- Routing: `parseRoute` tolera query/`%`-roto/vacíos; `legalHref` 4 formas; `TopBar` título legal genérico. Settings: `mergeSettings` enumera `legal` (`settingsStore.ts:529`), `migrateLegal` sanea enums/presupuestos.
- `idb.ts` upgrade idempotente (`contains`), `blocked/blocking` cierran conexión; `sw.js` bypass + `?v=hash`/`?t=` correctos en raíz.
- `analysisStore`: doble run rechazado con `null`, `stop()` pre-llamadas invalida token, degradación N personas + nota, quota con `ANALYSIS_QUOTA_ERROR` sin tumbar.
- `caseStore`: errores a `status/error` sin lanzar; `remove` optimista con rollback.

## Notas de alcance

- No se re-ejecutó `tsc`/`test`/`build` (entorno sin toolchain operativa); la base "1319 tests verdes" se toma del board.
- `R5` (`rename` usa `Date.now()` en vez del clock inyectado, `conversationsStore.ts:137`) es preexistente y fuera del alcance legal; se menciona y no se toca.
